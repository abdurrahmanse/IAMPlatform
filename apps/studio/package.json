{
  "name": "studio",
  "version": "0.0.0",
  "scripts": {
    "dev": "DATABASE_URL=\"postgresql://postgres:45683968@localhost:5432/iamplatform\" prisma studio --config ../../packages/database/prisma.config.ts --port 3005",
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false"
  },
  "devDependencies": {
    "prisma": "7.4.2"
  }
}
